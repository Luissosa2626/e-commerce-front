- Para la parte del front se utiliza componentes estilizados

INICIAR
- yarn create next-app . (El punto para que lo cree en el directorio exacto donde esta)
- Editamos el index.js, global.css y home.module.css
- Algunos estilos globales los agrego en _app.js
- Cambiar la fuente - https://fonts.google.com/specimen/Roboto

YARN ADD
styled-components - Parecido a lo utilizado en Svelte para crear los estilos en la misma pagina
mongoose - Para conexion a base de datos MongoDB
stripe - Para almacenar informacion de pago de los clientes en el navegador
micro - usado en pages>webhook.js para poder enviar notificaciones a los usuarios sobre su pedido
react-flying-item - para hacer animaciones de que el item seleccionado se mueva hacia el carrito de compras 
next-reveal - para items que quieras darle animaciones
lodash - funciones de programacion
next-auth @next-auth/mongodb-adapter


NOTAS
- Pase el archivo mongoose.js y models>Product.js al front para poder llamar desde la base de datos
- Luego en el index.js, en la funcion getServerSideProps() hago las peticiones

IMPORTANTE
Esto es por que Mongo no es compatible con JSON directamente
- Para errores de serialized as JSON debo hacer el JSON.stringify - index.js
- Sino, trato con JSON.parse(JSON.stringify(product))
- En cart.js de pages: Dentro del form es importante el name y tambien el value y el onChange para que se puedan escribir valores. El value y el onChange estan estrechamente relacionados
- Para la session, paso el session provider en pages>_app.js (Para esto utilice mongodb.js, [...nextauth].js, _app.js) 
- Esta session esta en google console en el puerto 4000, next e-commerce-front
- Primero se trabaja el model (Lo que este en esa carpeta) despues se trabaja el api(lo que este en esa carpeta)


Para errores de:
Warning: Received `true` for a non-boolean attribute `primary`.
If you want to write it to the DOM, pass a string instead: primary="true" or primary={value.toString()}.

Debo agregarle outline={1} white={1}, es decir, un valor boolean a/ las props.

- Para errores de  Cannot read properties of undefined (reading 'length') puede agregar un ? antes del . 
newProducts?.length

- Para errores de 0 is not a function - es por que debo agregar && (cartProducts?.length > 0 &&)

- Para error de buscar info de Account era por que necesitaba llenar al menos el campo Name. Por que es required,desde que lo llene funciona



ERRORES vid 18:
FlyingButton
ProductBox
product>[id].js
Feature
Header
Pages>cart.js

- El boton de la pantalla principal no hace la animacion (el 1ro que esta al lado de READ MORE) - No carga por que esa justamente no tiene imagen
- Hay un articulo en cart que esta pegado y hace ver como que hay un articulo aunque no haya nada
- Tambien da un error al momento de pagar
- Con la pantalla pequenia cuando entro a products o categories y vuelvo abrir el hamburguer se queda pegado el fondo